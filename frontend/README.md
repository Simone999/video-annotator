# Frontend Placeholder

This directory is reserved for the Milestone 0 frontend scaffold described in
`docs/spec.md`.

Later milestones will add the React + TypeScript + Vite app and the empty
two-pane shell.

The frontend now includes only the developer-tooling baseline needed for
Prettier, ESLint, and TypeScript checks. No React or Vite application scaffold
exists here yet.
