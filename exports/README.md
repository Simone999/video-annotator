# Exports Directory Placeholder

This directory is reserved for local-only export outputs generated by the
application.

Backend export services can now materialize deterministic package roots with
`annotations.json` and optional `masks/*.png` artifacts. API routes and UI
download flow are not wired yet, so this directory still mainly documents the
intended repository layout.
